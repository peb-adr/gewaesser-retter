Work in progress:

uuidPublic: unique identifier
aktionsname: display name
type:
 * trash : collected trash
 * campaign / closed/ finding: a trash collection event
datum:
uhrzeit:
veranstalter:
beschreibung:

url: external url to show details not in database (e.g. contacts) - if present,
  it will try to open in an optional small iframe in the info box

